# Версии зафиксированы намеренно точно (==), а не диапазонами — воспроизводимые
# сборки на любом PaaS. requires-python в pyproject.toml не даёт pip/uv выбрать
# Python 3.14 (для него ещё нет готовых wheel-ов pydantic-core -> сборка через
# Rust/maturin падает без тулчейна на большинстве PaaS).
fastapi==0.110.0
uvicorn[standard]==0.27.0
python-docx==0.8.11
jinja2==3.1.3
natasha==1.6.0
pydantic==2.6.4
pydantic-settings==2.2.1
slowapi==0.1.10
